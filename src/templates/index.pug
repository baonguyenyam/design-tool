extends _layout/layout

//- Các giá trị mặc định cho trang web
block variables
	- var title = 'Home'
	- var description = 'Description for home page'
	- var bodyclass = 'home'

//- Phần khai báo body
block body
	#loading
		.bar Đang tải dữ liệu...
		.progress
			.progress-bar.progress-bar-striped.bg-secondary.progress-bar-animated(role="progressbar" aria-valuenow="100" style="width: 100%" aria-valuemin="0" ariavaluemax="100")

	section.canhcam-design-1
		.container-fluid-full
			.row.no-gutters
				.col-8
					.box-product Lorem ipsum dolor, sit amet consectetur adipisicing elit. Officiis doloribus animi reprehenderit itaque blanditiis eius, consectetur accusantium porro libero iste ad dicta vitae asperiores necessitatibus fugiat error? Sapiente, alias recusandae.
				.col-4.select(ng-controller="getMenuMaterial")
					.select-color
						ul
							//- li.item(ng-repeat="item in menus", ng-class="{active: $index == 0 }")
							li.item(ng-repeat="item in menus")
								a(href="javascript:void(0);" onclick="toggleMenu(this)" ng-click="ctrlClickHandler(item.get)") 
									em(ng-bind-html="item.icon | html")
									span {{item.name}}
					.select-nav
						//- .select-nav-slider
							.owl-carousel.owl-theme
								.item
									a.btn-title(href="#") Demo
								.item.active
									a.btn-title(href="#") Demo
								.item
									a.btn-title(href="#") Demo
								.item
									a.btn-title(href="#") Demo
								.item
									a.btn-title(href="#") Demo
								.item
									a.btn-title(href="#") Demo
								
						//- hr 
						.select-nav-color
							h3.h5.mt-2.mb-0 Danh mục chất liệu
							.alert.alert-info.mt-3(ng-show="!materials.length && !showloading") Không có danh mục nào cả!
							.showloading.mt-3(ng-show="showloading") 
								i.fa.fa-sync.fa-spin.mr-1
								| 
								| Đang tải dữ liệu...
							.list-item
								.item(ng-repeat="item in materials")
									em(ng-bind-html="item.icon | html")


